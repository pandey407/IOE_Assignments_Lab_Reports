		ORG 00H

; DATA FROM QUESTION
		MOV 60H,#0A5H
		MOV 61H,#0FDH
		MOV 62H,#67H
		MOV 63H,#42H
		MOV 64H,#0DFH
		MOV 65H,#9AH
		MOV 66H,#84H
		MOV 67H,#1BH
		MOV 68H,#0C7H
		MOV 69H,#31H
		
; MAIN ROUTINE FOR SORTING
		MOV R0,#60H 
		MOV R6,#09H 
AGN:	ACALL F_LARGE
		MOV @R0,A
		INC R0
		DJNZ R6,AGN

; DISPLAYING SORTED LIST
AGAIN:	MOV R1,#0AH
		MOV R0,#60H
LOOP:	MOV A,@R0
		MOV P0,A
		ACALL DELAY
		INC R0
		DJNZ R1,LOOP
		AJMP AGAIN
		
; SELECTION SORT ALGORITHM		
F_LARGE:MOV B,R0
		MOV A,R6
		MOV R2,A 
	
		MOV A,@R0
		MOV R1,A 
		
NEXT:	INC R0
		MOV R4,A 
		SUBB A,@R0
		JNC SKIP
		
		MOV A,@R0
		MOV R1,A
		MOV A,R4
		MOV @R0,A
		
SKIP:	MOV A,R1
		DJNZ R2,NEXT
		MOV R0,B
		RET

DELAY: 	MOV R3,#7
LOOP1:	MOV R4,#255
LOOP2: 	MOV R5,#255
LOOP3:	DJNZ R5,LOOP3
		DJNZ R4,LOOP2
		DJNZ R3,LOOP1
		RET
		
		END