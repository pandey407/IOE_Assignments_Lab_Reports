		ORG 00H
		
		MOV 22H,#0FFH
		MOV 15H,#0DEH
		
		MOV A,#0H
		MOV R1,#0H
		
		MOV R0,22H
AGAIN:	ADD A,15H
		JNC SKIP
		INC R1
SKIP:	DJNZ R0,AGAIN

		MOV 19H,A
		MOV 1AH,R1
		
LOOP:	MOV P0,A
		ACALL DELAY
		MOV P0,R1
		ACALL DELAY
		AJMP LOOP

DELAY: 	MOV R4,#7
LOOP1:	MOV R5,#255
LOOP2: 	MOV R7,#255
LOOP3:	DJNZ R7,LOOP3
		DJNZ R5,LOOP2
		DJNZ R4,LOOP1
		RET
		
		END
		